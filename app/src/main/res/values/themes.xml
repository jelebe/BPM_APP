<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base -->
    <style name="Theme.BesosBPM" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Color de fondo de la barra superior -->
        <item name="colorPrimary">@color/toolbar_background</item>
        <item name="colorPrimaryVariant">@color/pink_dark</item>

        <!-- Color del texto y los íconos en la barra superior -->
        <item name="android:textColorPrimary">@color/toolbar_text_color</item>
        <item name="actionMenuTextColor">@color/toolbar_text_color</item>
        <item name="android:icon">@color/toolbar_icon_color</item>

        <!-- Otros atributos -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
    </style>
</resources>